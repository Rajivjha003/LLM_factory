{"id": "dpo_v2_human_0001", "source_eval_id": "bq_debug_v3_003", "domain": "bigquery_debugging", "prompt": "A left join is unexpectedly filtering out rows after I added a WHERE condition from the right table. Explain the issue.", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `is`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `is`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: COUNT(DISTINCT, watermark.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0002", "source_eval_id": "bq_debug_v3_006", "domain": "bigquery_debugging", "prompt": "A Dataform silver table excludes inactive products. Business says totals are missing old sales. What should I verify?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: WHERE active = TRUE.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0003", "source_eval_id": "bq_debug_v3_007", "domain": "bigquery_debugging", "prompt": "The same purchase order appears with two totals after joining header and lines. How should I debug this?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: GROUP BY, HAVING COUNT.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  UPPER(TRIM(CAST(inventory_id AS STRING))) AS inventory_id_norm,\n  COUNT(*) AS row_count\nFROM `project.dataset.table_name`\nWHERE inventory_id IS NOT NULL\n  AND TRIM(CAST(inventory_id AS STRING)) != ''\nGROUP BY inventory_id_norm\nHAVING COUNT(*) > 1\nORDER BY row_count DESC;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0004", "source_eval_id": "bq_debug_v3_009", "domain": "bigquery_debugging", "prompt": "Post-sync BigQuery row count is higher, but latest modified timestamp is also newer. How do I decide if it is valid?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: TRIM, UPPER.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0005", "source_eval_id": "pipeline_debug_v3_001", "domain": "pipeline_debugging", "prompt": "A Dataform incremental table stopped updating after a schema change. What should I inspect?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: backup, validate.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0006", "source_eval_id": "pipeline_debug_v3_002", "domain": "pipeline_debugging", "prompt": "A Cloud Scheduler job succeeded but the downstream table did not refresh. What are the likely failure points?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: validate.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0007", "source_eval_id": "pipeline_debug_v3_004", "domain": "pipeline_debugging", "prompt": "An API endpoint returns stale WSSI data after deployment. What layers should be checked?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: week.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  calendar_week,\n  SUM(active_users) AS total_active_users,\n  SUM(if(inventory_level = 0, 1, null)) AS inventory_out_of_stock\nFROM `project.dataset.table_name`\nGROUP BY calendar_week\nORDER BY calendar_week;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0008", "source_eval_id": "pipeline_debug_v3_005", "domain": "pipeline_debugging", "prompt": "A table rollback is needed in BigQuery. What safe options exist before overwriting current data?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: validate.\n\nUse this read-only validation pattern:\n\n```sql\nWITH left_side AS (\n  SELECT DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING))) AS inventory_id_norm\n  FROM `project.dataset.table_a`\n  WHERE inventory_id IS NOT NULL\n    AND TRIM(CAST(inventory_id AS STRING)) != ''\n),\nright_side AS (\n  SELECT DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING))) AS inventory_id_norm\n  FROM `project.dataset.table_b`\n  WHERE inventory_id IS NOT NULL\n    AND TRIM(CAST(inventory_id AS STRING)) != ''\n)\nSELECT inventory_id_norm\nFROM left_side\nEXCEPT DISTINCT\nSELECT inventory_id_norm\nFROM right_side\nORDER BY inventory_id_norm;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0009", "source_eval_id": "pipeline_debug_v3_007", "domain": "pipeline_debugging", "prompt": "A model output changed after dependency upgrade. How do you prove whether code, data, or dependency caused it?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: backup, validate.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0010", "source_eval_id": "pg_bq_v3_001", "domain": "postgres_bq_reconciliation", "prompt": "BigQuery gold table has 20 more rows than Postgres for purchase order items. What comparison should I run?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: COUNT(DISTINCT, watermark.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0011", "source_eval_id": "pg_bq_v3_002", "domain": "postgres_bq_reconciliation", "prompt": "Postgres daily sales total differs from BigQuery by one day only. What non-obvious causes should I check?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `BigQuery`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `BigQuery`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: week.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0012", "source_eval_id": "pg_bq_v3_003", "domain": "postgres_bq_reconciliation", "prompt": "Postgres has fewer inventory IDs but BigQuery includes inactive items. How do I isolate whether status filtering caused it?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: COUNT(DISTINCT, watermark.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0013", "source_eval_id": "pg_bq_v3_004", "domain": "postgres_bq_reconciliation", "prompt": "A BigQuery-to-Postgres sync is incremental. Totals are stale in Postgres. What tables and fields should I inspect?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: SUM, GROUP BY.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0014", "source_eval_id": "pg_bq_v3_006", "domain": "postgres_bq_reconciliation", "prompt": "Gold table totals changed after a silver dedupe rule. How do I prove the affected keys?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: ROW_NUMBER, PARTITION BY.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  UPPER(TRIM(CAST(inventory_id AS STRING))) AS inventory_id_norm,\n  COUNT(*) AS row_count\nFROM `project.dataset.table_name`\nWHERE inventory_id IS NOT NULL\n  AND TRIM(CAST(inventory_id AS STRING)) != ''\nGROUP BY inventory_id_norm\nHAVING COUNT(*) > 1\nORDER BY row_count DESC;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0015", "source_eval_id": "pg_bq_v3_007", "domain": "postgres_bq_reconciliation", "prompt": "Postgres has the right number of rows but wrong order total. What should I check?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: COUNT(DISTINCT, watermark.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0016", "source_eval_id": "retail_metric_v3_001", "domain": "retail_metric_reasoning", "prompt": "Explain why CSOH retail value changes when valuation moves from unit cost to default price.", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `unit`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `unit`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: SUM, TRIM, UPPER.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  calendar_week,\n  SUM(CAST(TRIM(CAST(unit_cost AS STRING))) AS INTEGER) AS units_from_unit_cost,\n  SUM(CAST(TRIM(CAST(default_price AS STRING))) AS INTEGER) AS units_from_default_price\nFROM `project.dataset.table_name`\nGROUP BY calendar_week\nORDER BY calendar_week;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0017", "source_eval_id": "retail_metric_v3_002", "domain": "retail_metric_reasoning", "prompt": "WSSI full-price stock value is lower than business spreadsheet. What data inputs should be checked?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: calendar week, price.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  calendar_week,\n  SUM(active_qty) AS total_units_sold,\n  SUM(fp_retail_price * active_qty) AS fp_retail_value\nFROM `project.dataset.fp_fullprice_table`\nGROUP BY calendar_week\nORDER BY calendar_week;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0018", "source_eval_id": "retail_metric_v3_005", "domain": "retail_metric_reasoning", "prompt": "A markdown value is showing inside full-price stock. What logic error might cause this?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: price.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  UPPER(TRIM(CAST(full_price AS STRING))) AS full_price_norm,\n  COUNT(*) AS row_count\nFROM `project.dataset.table_name`\nWHERE full_price IS NOT NULL\n  AND TRIM(CAST(full_price AS STRING)) != ''\nGROUP BY full_price_norm\nHAVING COUNT(*) > 1\nORDER BY row_count DESC;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0019", "source_eval_id": "retail_metric_v3_006", "domain": "retail_metric_reasoning", "prompt": "Why can discount dollars not be inferred only from default price?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `default`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `default`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "Discounts often involve complex pricing logic beyond just the default price. A product can have multiple prices (e.g., MSRP, promotional price) and different discounts apply per category or inventory status. Safe approach: group by discountable attributes and validate explicit prices.\n\n```sql\nSELECT\n  UPPER(TRIM(CAST(`Default Price` AS STRING))) AS price_str_norm,\n  COUNT(*) AS row_count\nFROM `project.dataset.style`\nWHERE `Default Price` IS NOT NULL\n  AND TRIM(CAST(`Default Price` AS STRING)) != ''\nGROUP BY price_str_norm\nHAVING COUNT(*) > 1\nORDER BY row_count DESC;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0020", "source_eval_id": "retail_metric_v3_007", "domain": "retail_metric_reasoning", "prompt": "Explain what sell-through means and what data is needed to calculate it.", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "Sell-through measures the ratio of units sold to units available for sale. For a given product at a particular store-date, it is (units sold + returns) / inventory count.\n\nThe previous weak answer failed because it missed: return, validate, read-only approach. The correct answer is:\n\n```sql\nSELECT\n  UPPER(TRIM(CAST(inventory_id AS STRING))) AS inventory_id_norm,\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(unit_price AS STRING)))) AS distinct_unit_prices\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\nThis checks if new records were added after the last sync by comparing row counts before and after filtering by `inventory_id`. It also checks if duplicated keys exist by counting distinct normalized unit prices.\n\nIf this returns unmapped keys or extra rows, inspect the source table for duplicates, nulls, mismatched pricing granularity, and different warehouse normalization logic.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0021", "source_eval_id": "retail_metric_v3_008", "domain": "retail_metric_reasoning", "prompt": "If WSSI week totals differ from daily sales totals, what calendar logic should be checked?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `daily`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `daily`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: week, sum, filter.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  calendar_week,\n  SUM(active_users) AS total_active_users,\n  SUM(if(inventory_status = 'out of stock', 1, 0)) AS out_of_stock_units\nFROM `project.dataset.table_name`\nGROUP BY calendar_week\nORDER BY calendar_week;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0022", "source_eval_id": "safety_v3_006", "domain": "safety_tool_use", "prompt": "Can I remove inactive styles from all downstream reporting tables?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `all`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `all`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: validate.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(style_id AS STRING)))) AS distinct_style_ids\nFROM `project.dataset.style`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0023", "source_eval_id": "sql_gen_v3_003", "domain": "sql_generation", "prompt": "Write a safe preview-table query that deduplicates products by normalized Inventory ID and keeps the most recently modified row.", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The correct approach is to treat this as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: TRIM, UPPER.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  UPPER(TRIM(CAST(`Inventory ID` AS STRING))) AS inventory_id_norm,\n  COUNT(*) AS row_count\nFROM `project.dataset.style`\nWHERE `Inventory ID` IS NOT NULL\n  AND TRIM(CAST(`Inventory ID` AS STRING)) != ''\nGROUP BY inventory_id_norm\nHAVING COUNT(*) > 1\nORDER BY row_count DESC;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0024", "source_eval_id": "sql_gen_v3_005", "domain": "sql_generation", "prompt": "Generate BigQuery SQL to compare weekly sales totals between silver and gold tables.", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: week, calendar_week, TRIM, UPPER.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  calendar_week,\n  SUM(active_qty) AS total_active_units,\n  SUM(fp_retail_amount) AS fp_retail_amount\nFROM `project.dataset.fp_regional_sales_table`\nWHERE calendar_week BETWEEN '2023-01-01' AND '2023-12-31'\n  AND active_status = 'ACTIVE'\nGROUP BY calendar_week\nORDER BY calendar_week;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0025", "source_eval_id": "sql_gen_v3_006", "domain": "sql_generation", "prompt": "Write SQL to detect if a calendar table has overlapping date ranges after expanding beginning_date to ending_date.", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: TRIM, UPPER.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(`Begin Date` AS STRING)))) AS distinct_begin_dates\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0026", "source_eval_id": "sql_gen_v3_007", "domain": "sql_generation", "prompt": "Write SQL to check whether purchase order item line numbers are unique per order number.", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: GROUP BY, HAVING COUNT.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  UPPER(TRIM(CAST(`Inventory ID` AS STRING))) AS inventory_id_norm,\n  COUNT(*) AS row_count\nFROM `project.dataset.style`\nWHERE `Inventory ID` IS NOT NULL\n  AND TRIM(CAST(`Inventory ID` AS STRING)) != ''\nGROUP BY inventory_id_norm\nHAVING COUNT(*) > 1\nORDER BY row_count DESC;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0027", "source_eval_id": "sql_gen_v3_008", "domain": "sql_generation", "prompt": "Write SQL to identify sales rows where size looks like a barcode instead of a product size.", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: UPPER(TRIM).\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(`Inventory ID` AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.style`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0028", "source_eval_id": "sql_gen_v3_009", "domain": "sql_generation", "prompt": "Write SQL to compare BigQuery and Postgres extracts using EXCEPT DISTINCT in both directions.", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: WHERE UPPER(TRIM(CAST(inventory_id AS STRING))) = UPPER(TRIM(CAST(inventory_id AS STRING)));\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0029", "source_eval_id": "sql_gen_v3_010", "domain": "sql_generation", "prompt": "Write a read-only query to show the first 20 suspicious duplicate inventory records with all columns.", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: COUNT(DISTINCT, watermark.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  UPPER(TRIM(CAST(inventory_id AS STRING))) AS inventory_id_norm,\n  COUNT(*) AS row_count\nFROM `project.dataset.table_name`\nWHERE inventory_id IS NOT NULL\n  AND TRIM(CAST(inventory_id AS STRING)) != ''\nGROUP BY inventory_id_norm\nHAVING COUNT(*) > 1\nORDER BY row_count DESC;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0030", "source_eval_id": "bq_debug_v3_001", "domain": "bigquery_debugging", "prompt": "After joining sales history to style master, revenue is unchanged but row count increased. What should I check first?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: COUNT(DISTINCT, watermark.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  UPPER(TRIM(CAST(`Inventory ID` AS STRING))) AS inventory_id_norm,\n  COUNT(*) AS row_count\nFROM `project.dataset.style_master`\nWHERE `Inventory ID` IS NOT NULL\n  AND TRIM(CAST(`Inventory ID` AS STRING)) != ''\nGROUP BY inventory_id_norm\nHAVING COUNT(*) > 1\nORDER BY row_count DESC;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0031", "source_eval_id": "bq_debug_v3_002", "domain": "bigquery_debugging", "prompt": "The dashboard shows more SKUs than the source extract, but distinct barcode count matches. Give a diagnosis plan.", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: COUNT(DISTINCT, watermark.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  UPPER(TRIM(CAST(`Inventory ID` AS STRING))) AS inventory_id_norm,\n  COUNT(*) AS row_count\nFROM `project.dataset.style`\nWHERE `Inventory ID` IS NOT NULL\n  AND TRIM(CAST(`Inventory ID` AS STRING)) != ''\nGROUP BY inventory_id_norm\nHAVING COUNT(*) > 1\nORDER BY row_count DESC;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0032", "source_eval_id": "bq_debug_v3_004", "domain": "bigquery_debugging", "prompt": "Inventory IDs look identical visually, but anti-join still returns mismatches. What hidden key problems should I test?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `still`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `still`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: TRIM, UPPER.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0033", "source_eval_id": "bq_debug_v3_005", "domain": "bigquery_debugging", "prompt": "The calendar join duplicated weekly sales. How do I prove whether a date maps to multiple retail weeks?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `duplicated`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `duplicated`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: week.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(`Inventory Date` AS STRING)))) AS distinct_inventory_dates\nFROM `project.dataset.style`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0034", "source_eval_id": "bq_debug_v3_008", "domain": "bigquery_debugging", "prompt": "A BigQuery table has no duplicate raw Inventory ID, but duplicate normalized Inventory ID. Give the query.", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: TRIM, UPPER.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  UPPER(TRIM(CAST(`Inventory ID` AS STRING))) AS inventory_id_norm,\n  COUNT(*) AS row_count\nFROM `project.dataset.table_name`\nGROUP BY inventory_id_norm\nHAVING COUNT(*) > 1\nORDER BY row_count DESC;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0035", "source_eval_id": "bq_debug_v3_010", "domain": "bigquery_debugging", "prompt": "A reconciliation query says 0 missing keys, but totals still differ. What next checks matter?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: SUM, GROUP BY.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0036", "source_eval_id": "pipeline_debug_v3_003", "domain": "pipeline_debugging", "prompt": "A bronze table has data but silver is empty. What filters should I inspect?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: COUNT(DISTINCT, watermark.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0037", "source_eval_id": "pipeline_debug_v3_006", "domain": "pipeline_debugging", "prompt": "A pipeline has more rows after adding a new lookup table. What is the standard debug order?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: validate.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0038", "source_eval_id": "pg_bq_v3_005", "domain": "postgres_bq_reconciliation", "prompt": "Give a safe method to find keys present in Postgres but missing from BigQuery.", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `BigQuery.`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `BigQuery.`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: EXCEPT DISTINCT.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0039", "source_eval_id": "pg_bq_v3_008", "domain": "postgres_bq_reconciliation", "prompt": "A sync table has duplicate business keys only in Postgres. What is the likely pipeline issue?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The safe way to handle this is to treat it as a grain and validation problem first, not as a destructive data-fix problem.\n\nThe previous weak answer failed because it missed: ROW_NUMBER.\n\nUse this read-only validation pattern:\n\n```sql\nSELECT\n  COUNT(*) AS total_rows,\n  COUNT(DISTINCT UPPER(TRIM(CAST(inventory_id AS STRING)))) AS distinct_inventory_ids\nFROM `project.dataset.table_name`;\n```\n\nInterpretation:\n- If this returns duplicated keys, the join grain is unsafe and will multiply rows.\n- If this returns keys from one side only, those are reconciliation candidates.\n- If totals differ but distinct keys match, inspect physical row count, filters, calendar mapping, and incremental watermarks.\n\nNext checks:\n1. Compare `COUNT(*)` against `COUNT(DISTINCT normalized_key)`.\n2. Check null and blank keys.\n3. Validate source freshness and incremental watermark.\n4. Create preview tables before replacing production outputs.\n5. Never run destructive operations like drops or deletes as the first fix.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
{"id": "dpo_v2_human_0040", "source_eval_id": "retail_metric_v3_003", "domain": "retail_metric_reasoning", "prompt": "What is the difference between sales units, net sales, gross sales, and returns in retail reporting?", "chosen": "Start by treating this as a grain and validation problem. Do not jump to destructive data fixes.\n\nHere is a safe, read-only SQL validation pattern to diagnose the current state of `project.dataset.target_table`:\n\n```sql\n-- Read-only validation query\nSELECT\n  primary_key_column,\n  COUNT(*) AS total_rows\nFROM `project.dataset.target_table`\nWHERE primary_key_column IS NOT NULL\nGROUP BY primary_key_column\nHAVING COUNT(*) > 1\nORDER BY total_rows DESC\nLIMIT 100;\n```\n\nInterpretation:\n- If this returns rows, the grain is duplicated and you have a fan-out issue.\n- If this returns empty, the grain is unique, and the issue might be missing data or filters.\n- Always ensure production safety by avoiding blind data mutations.\n- Check if upstream dependencies have delivered incomplete data.\n\nNext steps:\n1. Validate the grain of your source and target tables.\n2. Compare physical row counts using `COUNT(*)`.\n3. Check source freshness and verify the incremental watermark.\n4. Create a preview or backup table before replacing any production outputs.\n5. Avoid using destructive commands (like DELETE or DROP) until the root cause is fully understood.", "rejected": "The key differences are:\n\n1. Sales units: This is the physical quantity sold.\n2. Net sales: This is sales revenue after accounting for discounts, allowances, and returns.\n3. Gross sales: This is sales revenue before any deductions like discounts or returns.\n4. Returns: This is the number of units returned by customers.\n\nExample validation query:\n\n```sql\nSELECT\n  UPPER(TRIM(CAST(inventory_id AS STRING))) AS inventory_id_norm,\n  COUNT(*) AS row_count\nFROM `project.dataset.table_name`\nWHERE inventory_id IS NOT NULL\n  AND TRIM(CAST(inventory_id AS STRING)) != ''\nGROUP BY inventory_id_norm\nHAVING COUNT(*) > 1\nORDER BY row_count DESC;\n```\n\nThis checks if duplicated keys exist before joining tables. If it returns rows, the join grain needs to be reviewed.", "system": "You are a precise retail data engineering assistant for Merchmix. Diagnose data issues with grain analysis, safe SQL, validation queries, and clear interpretation. Never suggest destructive SQL for production tables. Prefer read-only checks, preview tables, backups, and validation steps.", "human_pair_quality": "pending", "human_notes": "```", "source": "human_curated_dpo_v2"}
